#!/bin/sh /etc/rc.common

START=96
STOP=10
USE_PROCD=1

start_service() {
	procd_open_instance main
	procd_set_param command /usr/share/openclash-editor/portal-watch.sh run
	procd_set_param respawn 5 5 0
	procd_set_param stdout 1
	procd_set_param stderr 1
	procd_close_instance
}

stop_service() {
	/usr/share/openclash-editor/portal-watch.sh cleanup >/dev/null 2>&1 || true
}

reload_service() {
	restart
}
